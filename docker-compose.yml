services:
  server:
    build: .
    command: python main.py
    volumes:
      - .:/code
    ports:
      - "2080:2080"
      - "2081:2081"
    restart: unless-stopped

networks:
  default:
    driver: bridge
